{% for content in contents %}
	<div class="container">
		<div class="row pt-5 align-items-center">
			<div class="col-lg-6">
				<img class="img-fluid rounded-circle shadow" src="{{ ('/uploads/pictures/' ~ content.image)|imagine_filter('size')}}" alt="{{ content.title }}">
				<h1 class="text-primary mt-5 text-center">{{ content.title }}</h1>
			</div>
			<div class="col-lg-6">
				<img class="img-fluid mx-auto" src="{{ asset('build/images/lotus.png') }}" alt="Bien être et Qi Gong" height="500px" width="500px">
				<p class="text-primary mt-5">{{ content.body|raw }}</p>
			</div>
		</div>
	</div>
{% endfor %}

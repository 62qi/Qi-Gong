<nav class="navbar navbar-expand-lg navbar-dark sticky-top" {% if page is defined %} style="background:{{ page.color }}" {% endif %} id="nav">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav justify-content-around w-100">
      <a class="nav-link text-white d-none d-lg-block " href="{{ path('app_home') }}"><img src="{{ asset('build/images/logonenuf.png') }}" alt="zen" width="80px"></a>
        {% for link in navigation.getNavigation %}
              <a class="nav-link text-white fs-5" href="{{ path('page', {'slug': link.slug}) }}">{{ link.linkNameNav }}</a>
          {% endfor %}
            {% if is_granted('ROLE_ADMIN') %}
              <a class="nav-link text-white fs-5" href="{{ path('admin_index') }}">Administration</a>
            {% endif %}
      </div>
    </div>
</nav>


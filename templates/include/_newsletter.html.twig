	{{ form_start(form) }}
	<h5 class="text-uppercase mb-4 text-center mb-4 text-primary">Inscription à ma Newsletter</h5>
	<div class="form-outline form-white pe-5 ps-5">
		{{ form_widget(form) }}
	</div>
	<div class="d-flex justify-content-center">
		<button type="submit" class="btn btn-primary">Inscription</button>
	</div>
	{{ form_end(form) }}

